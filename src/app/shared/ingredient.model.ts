export class Ingredient {

    constructor(public name: string, public amount: number) { } //short like bottom
    // public name: string;
    // public amount: number;

    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // }

}